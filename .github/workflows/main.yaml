name: memverse

# Ensure consistent concurrency settings across workflows
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  # TODO: Uncomment and fix the extract-config job when dynamic config extraction is working properly
  # extract-config:
  #   runs-on: ubuntu-latest
  #   outputs:
  #     min_coverage: ${{ steps.extract.outputs.min_coverage }}
  #   steps:
  #     - uses: actions/checkout@v4
  #     - id: extract
  #       name: Extract configuration values
  #       run: |
  #         # Extract MIN_COVERAGE from project_config.sh
  #         MIN_COVERAGE=$(grep "MIN_COVERAGE=" scripts/project_config.sh | cut -d'=' -f2)
  #         # Output as integer (no quotes)
  #         echo "min_coverage=$(echo $MIN_COVERAGE | tr -d '"' | tr -d "'")" >> $GITHUB_OUTPUT
  #         echo "Using min_coverage: $(echo $MIN_COVERAGE | tr -d '"' | tr -d "'")"

  build:
    # needs: extract-config
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/flutter_package.yml@v1
    with:
      flutter_channel: stable
      format_directories: lib/src test
      # TODO: Fix dynamic extraction of min_coverage from config - currently using hardcoded value
      min_coverage: 97
      coverage_excludes: "lib/l10n/arb/**"
      format_line_length: 100

  spell-check:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/spell_check.yml@v1
    with:
      includes: |
        **/*.md
      modified_files_only: false
